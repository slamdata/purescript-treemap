{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build --censor-lib --strict",
    "example-static": "pulp browserify -I examples/static/src --main Example.Static --to examples/static/dist/example.js",
    "example-random": "pulp browserify -I examples/random/src --main Example.Random --to examples/random/dist/example.js"
  },
  "devDependencies": {
    "pulp": "^9.0.1",
    "purescript-psa": "^0.3.9",
    "rimraf": "^2.5.4",
    "virtual-dom": "^2.1.1"
  }
}
